import React from 'react';
import '../../css/App.css'; // Make sure the CSS file contains the 3D effect

function Logo() {
    return (
        <img src="logo.png" alt="Logo" className="logo-img" />
    );
  }
  
 export default Logo;